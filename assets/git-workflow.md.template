# Git 与多 Agent 协作规范

## 基本原则

- `main` 是稳定集成分支，只保留已完成、验证通过且可正常运行的代码。
- 小型、低风险修改可以由一个任务直接在 `main` 完成并立即提交；一般功能、跨模块改动和数据库变更使用功能分支。
- 分支按功能划分，不按技术层机械拆分；同一功能分支可以同时修改前端、后端、测试和文档。

## 并行开发

多个 Agent 可以同时开发同一项目，但每个任务必须使用独立分支、独立 worktree 和独立物理目录。同一时间只能有一个任务直接使用 `main` 工作目录。

开始前应明确每个任务的分支、worktree、修改范围、共享契约、验证命令和完成标准。多个任务共同开发大型功能时，先确定 API、数据结构和错误格式等共享契约，再将各任务分支合入功能集成分支；联调和完整验证通过后才合入 `main`。

## 提交与合并

修改前检查工作区状态和当前分支。只暂存本任务明确修改的文件，多任务协作时谨慎使用全量暂存，不得提交、覆盖或回退其他任务的改动。

冲突需要理解双方实现后人工整合，重点检查接口、数据库迁移、公共类型、依赖和启动配置。合入 `main` 前，应运行与改动范围匹配的测试、静态检查、迁移检查和构建。

功能合并并验证通过后，使用 Git 命令移除 worktree，再删除已合并的短期分支。删除分支不会删除已经进入 `main` 的提交历史。

对话 Fork 只复制任务上下文，不等于 Git 分支。需要并行修改代码时，应同时使用 Git 分支和 worktree。
