# Agent 工作入口

本文件是 Agent 在本仓库的唯一默认入口。先读本文件，再按需加载其他文档。

## 项目简介

[用 2-5 句话说明：这个项目解决什么问题、服务谁、核心边界是什么。]

## 技术栈与目录职责

- `[dir]/`：[职责，例如后端服务、前端应用、共享组件、脚本、测试等。]
- `[dir]/`：[职责]
- `docs/`：技术文档、接口契约、架构说明和重要协作规则。


## 文档导航

- `docs/agent-workflow.md`：Agent 协作规则、plan/state 详细规则。通常无需读取，只有用户要求看协作规则、整体方案、当前进度，或需要创建/更新 `plan.md`、`state.md` 时读取。
- `docs/git-workflow.md`：Git 分支、worktree、提交、合并和多 Agent 并行开发规则。
- `[docs/xxx.md]`：[文档用途；如接口契约、数据结构、架构说明、外部系统交接等。]

文档导航维护规则：

- `/docs` 里的有效技术文档都应该能从本导航找到。
- 文档较少时，逐条列出所有正式文档；文档较多时，可以按目录分类列入口。
- 临时草稿、废弃文档、自动生成产物、历史备份可以不逐条列出，但应放在明确目录，例如 `docs/archive/`。
- `docs/agent-workflow.md` 必须始终存在，用于记录 Agent 协作规则、plan/state 详细规则。
- `docs/git-workflow.md` 必须始终存在，用于记录 Git 和并行开发规则。
- 新增、删除或移动 `/docs` 下文档时，必须同步更新本导航。

## 项目约束

- [不可违反的业务规则、产品边界或架构约束 1。]
- [不可违反的业务规则、产品边界或架构约束 2。]
- [如果没有特殊约束，可以写：暂无项目特定约束，按现有代码结构和文档约定执行。]

## 工作规则摘要

- 不自动读取 `plan.md`；用户要求看整体方案时才读。
- 不自动读取 `state.md`；用户要求看当前进度时才读。
- 只有大型 PRD、新开发阶段、跨模块整体变化或用户明确要求时才创建/更新 `plan.md`；如已有 plan，先询问是否覆盖或重建。
- 小需求、配置调整、bugfix、文档修正和日常实现细节不读取也不更新 `plan.md`。
- 用户明确要求更新 `state.md` 时，覆盖重写，控制在 500 字以内。
- 新增公共组件、工具函数、数据结构、接口格式、第三方依赖或发生架构调整时，必须更新 `/docs` 下对应文档。
- 仅修改组件内部逻辑、样式、业务文案，或 bugfix 不改变原有接口和结构时，不需要更新 `/docs`。
- 新增、删除、移动 `/docs` 文档时，同步更新本文件的文档导航。

## Git 与并行开发

- `main` 只保留已完成、验证通过且可正常运行的代码；一般功能使用功能分支。
- 分支按功能划分，可以同时包含前端、后端、测试和文档改动。
- 多个 Agent 并行开发同一仓库时，必须使用独立分支和独立 worktree，不得共享同一物理目录或未提交状态。
- 多任务共同开发大型功能时，先确定共享契约，再合入功能集成分支；联调验证通过后才合入 `main`。
- 修改和提交前检查工作区，只暂存本任务文件，不得覆盖、回退或提交其他任务的改动。
- 完整规则见 `docs/git-workflow.md`。

## 环境与配置

- [运行环境，例如 Node.js / Python / Go / Java 版本。]
- [包管理器、虚拟环境、数据库、外部服务配置说明。]
- 所有外部地址、密钥、环境差异配置走环境变量，不写死。


## 验证命令

```bash
[测试命令]
[lint 命令]
[build 命令]
```
