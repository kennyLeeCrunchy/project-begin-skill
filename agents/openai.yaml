interface:
  display_name: "Project Begin"
  short_description: "Initialize project Agent docs and workflow safely."
  default_prompt: "Use $project-begin to initialize this project with the reusable Agent documentation template."
policy:
  allow_implicit_invocation: true
