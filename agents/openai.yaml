interface:
  display_name: "Project Begin"
  short_description: "Initialize project Agent docs once; keep daily work in AGENTS.md."
  default_prompt: "Use $project-begin once to initialize or repair this project's Agent documentation and workflow."
policy:
  allow_implicit_invocation: true
